# Creator bypass
if 'varszz' in text.lower() or 'pembuat' in text.lower():
    self.vip_mode = True
    return "✅ LANGSUNG VIP BOS! (CREATOR MODE)"

# Normal flow dengan key
if not self.vip_mode:
    if 'minta akses' in text.lower():
        return '🔑 KEY NYA?'
    if 'varszzkey' in text.lower():
        self.vip_mode = True
        return '✅ BENAR! VIP AKTIF!'
    return '❌ PAKE LINK DULU BOS!'

# VIP mode
return f'```\n{text}\n```\n🔥 ADA LAGI?'